import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: {
    absolute: "Second",
  },
};

const Second = () => {
  return <h1>Second</h1>;
};

export default Second;
